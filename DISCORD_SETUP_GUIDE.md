# 📱 디스코드 웹훅 생성 및 스마트폰 실시간 알림 100% 수신 가이드

본 가이드는 KOBUS 고속버스 취소표 알리미 및 서울시립대학교(UOS) 수강신청 알리미에서 **빈자리 발생 시 스마트폰 잠금화면으로 1.5초 간격 3연타 모바일 진동/소리 푸시 알림**을 받기 위한 디스코드 연동 전 과정을 다룹니다.

---

## 📌 목차
1. [1단계: 나만의 디스코드 개인 서버 & 채널 생성](#1단계-나만의-디스코드-개인-서버--채널-생성)
2. [2단계: 웹훅(Webhook) URL 발급 및 복사](#2단계-웹훅webhook-url-발급-및-복사)
3. [3단계: 🚨 스마트폰 알림 100% 수신을 위한 필수 세팅 (핵심)](#3단계--스마트폰-알림-100-수신을-위한-필수-세팅-핵심)
4. [4단계: 알리미 프로그램에 웹훅 적용 및 테스트](#4단계-알리미-프로그램에-웹훅-적용-및-테스트)
5. [5단계: 문제 해결 (FAQ & 트러블슈팅)](#5단계-문제-해결-faq--트러블슈팅)

---

## 1단계: 나만의 디스코드 개인 서버 & 채널 생성

웹훅을 등록할 전용 디스코드 서버(무료)를 생성합니다. (PC 또는 모바일 앱 공통)

1. 디스코드 화면 왼쪽 서버 목록 맨 아래의 **`+` (서버 추가하기)** 버튼을 클릭합니다.
2. 템플릿 창에서 **[직접 만들기]**를 선택합니다.
3. **[나와 친구들을 위한 서버]**를 선택합니다.
4. 서버 이름(예: `내 실시간 알리미`)을 입력하고 **[만들기]**를 클릭합니다.
5. 생성된 서버의 채널 목록에서 기본 `#일반` 채널을 사용하거나, `+`를 눌러 `#알림` 텍스트 채널을 새로 생성합니다.

---

## 2단계: 웹훅(Webhook) URL 발급 및 복사

웹훅은 외부 프로그램(알리미 봇)이 내 디스코드 채널로 메시지를 쏠 수 있게 해주는 고유 주소입니다.

> 💡 **PC 디스코드 앱 또는 웹 브라우저에서 진행하는 것을 권장합니다.**

1. 알림을 수신할 채널 이름(예: `#알림`) 우측의 **톱니바퀴 아이콘 (채널 편집)**을 클릭합니다.
2. 좌측 메뉴에서 **[연동 (Integrations)]**을 클릭합니다.
3. **[웹훅 (Webhooks)]** 항목을 선택하고 **[새 웹훅 만들기]** 버튼을 클릭합니다.
4. 봇의 이름을 원하는 이름(예: `취소표·수강신청 3연타 알리미`)으로 지정합니다.
5. 하단의 **`웹훅 URL 복사 (Copy Webhook URL)`** 버튼을 클릭합니다.
   - 복사된 주소 형식:  
     `https://discord.com/api/webhooks/123456789012345678/abcdefg...`

---

## 3단계: 🚨 스마트폰 알림 100% 수신을 위한 필수 세팅 (핵심)

알리미 봇은 취소표 발생 시 사용자가 다른 일을 하거나 폰을 주머니에 넣어두었을 때도 즉시 인지할 수 있도록 **`@everyone` 멘션과 함께 1.5초 간격으로 3회 연속 진동 푸시**를 발송합니다. 스마트폰에서 알림이 누락되지 않도록 아래 항목을 반드시 점검하세요.

### 1) 디스코드 앱 내부 알림 설정 (스마트폰 앱)
1. 스마트폰 디스코드 앱에서 생성한 **서버 아이콘을 길게 터치**합니다.
2. 메뉴에서 **[알림 설정]**을 선택합니다.
3. **서버 알림 수준**: 반드시 **`모든 메시지 (All Messages)`**를 체크합니다.
4. **멘션 억제 설정 확인**:
   - **`@everyone 및 @here 억제`** 항목이 **꺼져 있는지 (비활성화)** 확인합니다.  
     *(이 옵션이 켜져 있으면 멘션 알림이 무음 처리됩니다!)*

### 2) PC 사용 중에도 스마트폰으로 즉시 알림 오게 만들기
디스코드는 기본적으로 PC에 접속 중이면 스마트폰 알림을 몇 분간 지연시킵니다. 이를 방지하려면:
1. 디스코드 **사용자 설정 (내 프로필 옆 톱니바퀴)** ➔ **[알림]** 메뉴로 이동합니다.
2. **`모바일 푸시 알림 시간 초과 (Push Notification Inactive Timeout)`** 설정을 찾습니다.
3. 기본값(예: 5분~10분)을 **`1분` 또는 `즉시`**로 변경합니다.

### 3) 스마트폰 OS 시스템 알림 권한 허용
* **아이폰 (iOS)**:
  - `설정` ➔ `알림` ➔ `Discord` 이동
  - `알림 허용`, `즉시 전달`, `잠금화면`, `알림 센터`, `배너`, `사운드` 모두 활성화
* **갤럭시 (Android)**:
  - `설정` ➔ `애플리케이션` ➔ `Discord` ➔ `알림` 이동
  - `알림 허용`, `소리 및 진동 허용`, `잠금화면 알림: 콘텐츠 표시` 활성화
  - `배터리 사용량 최적화`에서 Discord를 제외하면 화면 꺼짐 상태에서도 0초 딜레이로 울립니다.

---

## 4단계: 알리미 프로그램에 웹훅 적용 및 테스트

발급받은 웹훅 URL을 프로그램에 입력하고 테스트 알림을 발송합니다.

### 방법 1. 웹 GUI 대시보드에서 원클릭 연동 (추천 ⭐)
1. `run_gui.bat`을 실행하여 브라우저 대시보드를 엽니다.
   - 고속버스 알리미: `http://localhost:8081`
   - 시립대 수강 알리미: `http://localhost:8080`
2. 우측 상단의 **`[⚙️ 설정]`** 버튼을 클릭합니다.
3. **`디스코드 웹훅 URL`** 입력란에 복사한 주소를 붙여넣습니다.
4. **`[🔔 테스트 발송]`** 버튼을 클릭합니다.
5. 스마트폰 디스코드 앱으로 **`✅ [테스트] 알리미 연동 성공!`** 푸시 알림이 오는지 확인합니다.
6. **`[저장하기]`**를 클릭하면 설정이 영구 저장됩니다.

### 방법 2. `config.json` 파일에서 직접 설정
에디터로 `config.json`을 열어 `discord` 항목을 수정합니다:
```json
"discord": {
  "enabled": true,
  "webhook_url": "https://discord.com/api/webhooks/YOUR_WEBHOOK_ID/YOUR_WEBHOOK_TOKEN"
}
```

---

## 5단계: 문제 해결 (FAQ & 트러블슈팅)

| 증상 | 원인 | 해결 방법 |
|---|---|---|
| **PC 디스코드에는 메시지가 뜨는데 폰에 알림이 안 와요.** | 디스코드의 모바일 알림 지연 정책 | 디스코드 설정 ➔ 알림 ➔ `모바일 푸시 알림 시간 초과`를 1분/즉시로 변경하세요. |
| **알림은 오는데 진동/소리가 안 울려요.** | 멘션 억제 또는 무음 모드 | 서버 알림 설정에서 `@everyone 억제`가 꺼져 있는지, 폰 진동 모드가 켜져 있는지 확인하세요. |
| **`[!] 디스코드 전송 실패: HTTP Error 404`가 떠요.** | 웹훅 URL이 잘못되었거나 삭제됨 | 디스코드 채널 설정 ➔ 연동 ➔ 웹훅에서 URL을 다시 복사하여 붙여넣으세요. |
| **스마트폰 방해금지 모드 중에도 받고 싶어요.** | OS 방해금지 필터링 | 폰 설정 ➔ 집중 모드/방해금지 ➔ `허용된 앱`에 Discord를 등록하세요. |
