version: '3.8'

services:
  alert-hub:
    build: .
    container_name: realtime-alert-hub
    restart: always
    ports:
      - "8000:8000"
    environment:
      - TZ=Asia/Seoul
      - DOCKER_CONTAINER=1
    volumes:
      - ./data:/app/data   # 사용자 프로필 · 설정 · 관리자 키 영속화
